# Tasks

1. document UTC daily partitioning as the current policy
2. define migration constraints for timezone-aware daily keys
3. align replay and backfill semantics with the active policy
4. introduce compatibility guards before changing production behavior
