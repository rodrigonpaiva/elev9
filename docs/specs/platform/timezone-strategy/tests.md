# Tests

Future validation should cover:

- snapshots generated on day boundaries
- replay on the same record after midnight
- historical rebuild under a different timezone policy
- consistent date keys across engines

