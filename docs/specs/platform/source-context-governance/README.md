# SourceContext Governance

## Overview

This spec defines how `sourceContext` may be used across platform read models.

## Goal

Keep `sourceContext` useful for replay and debugging without turning it into a raw payload sink.

## Scope

Included:

- governance rules
- allowed and forbidden uses
- evolution strategy

Not included:

- schema redesign
- breaking API changes
- payload expansion

