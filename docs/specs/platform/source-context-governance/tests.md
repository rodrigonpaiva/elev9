# Tests

Future validation should cover:

- reduced snapshot shape only
- no raw payload leakage in public DTOs
- replay works with the reduced snapshot
- prompt builders do not receive raw context by default

