# Errors

Expected failure modes:

- source context bloat
- accidental raw payload persistence
- prompt injection risk through unfiltered context reuse
- replay drift caused by incomplete reduced context
