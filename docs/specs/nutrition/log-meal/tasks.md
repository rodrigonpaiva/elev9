# Tasks

- Add `NutritionLog` entity.
- Add `NutritionLogRepository`.
- Add `nutrition_logs` schema and indexes.
- Add `LogMealUseCase`.
- Add `POST /nutrition/logs`.
- Add shared request/response types.
- Add tests for status and progress behavior.
