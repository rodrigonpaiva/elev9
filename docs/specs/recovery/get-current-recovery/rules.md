## Rules

- The endpoint is read-only.
- The result must belong only to the authenticated user.
- The latest snapshot is selected by `date` and `createdAt`.
- The response should never expose other users' history.

