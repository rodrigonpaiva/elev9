## Tests

- returns today's snapshot when it already exists
- builds and persists today's snapshot when missing
- returns the same result on repeated reads
- respects user isolation
- handles empty history safely
