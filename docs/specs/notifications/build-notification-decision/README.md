# Build Notification Decision

## Overview

Builds the canonical deterministic `NotificationDecision` for the authenticated user.

```txt
Bounded Context: Notifications
Module: notifications
Use-case: build-notification-decision
Canonical name: notifications.notification-decision.build
```

## Goal

Turn platform signals into a single daily intervention decision.
