# Get Current Notification

## Overview

Returns the current canonical `NotificationDecision` for the authenticated user.

```txt
Bounded Context: Notifications
Module: notifications
Use-case: get-current-notification
Canonical name: notifications.notification-decision.get-current
```

## Goal

Expose the latest stable notification decision as the current state.
