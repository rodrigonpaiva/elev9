# Get Today Notification

## Overview

Returns today's persisted `NotificationDecision` for the authenticated user.

```txt
Bounded Context: Notifications
Module: notifications
Use-case: get-today-notification
Canonical name: notifications.notification-decision.get-today
```

## Goal

Expose the daily notification intervention state.
