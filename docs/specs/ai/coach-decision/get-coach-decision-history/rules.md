# Rules - Get Coach Decision History

- The history must be ordered newest first.
- The default limit is safe and small.
- The maximum limit prevents unbounded history reads.
- The response must not expose raw internal prompt payloads.
