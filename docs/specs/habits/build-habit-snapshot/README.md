# Build Habit Snapshot

## Overview

Builds the canonical daily `HabitSnapshot` for the authenticated user.

```txt
Bounded Context: habits
Module: habits
Use-case: build-habit-snapshot
Canonical name: habits.habit-snapshot.build
```

## Goal

Turn workout, recovery, nutrition, check-in, goal, notification, and coaching signals into a deterministic daily consistency snapshot.
