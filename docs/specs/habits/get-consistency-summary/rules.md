# Rules

- Use persisted habit snapshots as the source of truth.
- Do not recompute upstream engagement or workout history in the controller.
- Use deterministic aggregation only.
- Preserve user isolation.
- Keep replayability by deriving from persisted data.
