# Tests

- returns the active weight loss goal
- returns the active muscle gain goal
- returns the active consistency goal
- returns the active recovery goal
- fails when there is no active goal
- fails when the user profile is missing
- keeps the response stable and deterministic
