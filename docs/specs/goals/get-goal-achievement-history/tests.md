# Tests

- returns achievement history in descending order
- applies default limit 14
- applies maximum limit 90
- rejects invalid limits
- isolates data by user
- returns an empty list when no achievements exist
