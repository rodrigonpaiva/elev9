# Tests

- builds a progress snapshot for a weight loss goal
- builds a progress snapshot for a muscle gain goal
- builds a progress snapshot for a consistency goal
- builds a progress snapshot for a recovery goal
- falls back safely when signals are missing
- clamps progress to valid bounds
- persists via idempotent upsert
- keeps source context reduced
- records formula version
